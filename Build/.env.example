# Kopiere diese Datei nach Build/.env und trage deine lokalen Werte ein.
SIGNING_IDENTITY="${SIGNING_IDENTITY:-Developer ID Application: Vorname Nachname (TEAMID)}"
NOTARY_PROFILE="${NOTARY_PROFILE:-FluxBar-notary}"
